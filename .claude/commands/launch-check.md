---
description: Run the "before going live" checklist from README.md
---
Check the status of each go-live item and report done/not-done with a file:line reference:

1. **`og:url` meta tag** in `index.html` — still the placeholder `https://example.com/`, or updated to the real domain?
2. **Contact form** — does `<form id="contact-form">` have a non-empty `data-formspree-id`?
3. **Portfolio placeholders** — how many `work-card--placeholder` cards remain in the "Selected Work" section?
4. **Testimonials** — is the `.testimonials` section still all-placeholder quotes, or has it been replaced/removed?
5. **Social links** — do the footer and `.social-dock` links still point to `href="#"`?

Finish with a short summary of what's left before the site is ready to launch.

---
description: List outstanding <!-- EDIT ME --> placeholders left to fill in
---
Search `index.html` for every `<!-- EDIT ME -->` comment marker. For each one found, report:

- The file:line it's at
- What real content needs to replace it (based on the surrounding markup and the matching instructions in README.md, e.g. "Activating the contact form," "Adding portfolio work," "Adding testimonials," "Social links," "Before going live")

Present the result as a checklist, most-important-first (things a visitor would notice, like the contact form and portfolio placeholders, before cosmetic ones like `og:url`).
